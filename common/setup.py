from setuptools import setup, find_packages

setup(
    name='common',
    description='Common resources used throughout the project.',
    version='0.1.0',
    install_requires=[],
    packages=find_packages()
)
